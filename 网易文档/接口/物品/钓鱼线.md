---
title: 钓鱼线
order: 1
source_url: "https://mc.163.com/dev/mcmanual/mc-dev/mcdocs/1-ModAPI/%E6%8E%A5%E5%8F%A3/%E7%89%A9%E5%93%81/%E9%92%93%E9%B1%BC%E7%BA%BF.html"
last_modified: "Wed, 29 Apr 2026 14:40:01 GMT"
synced_from: "NetEase developer official website"
---

#  钓鱼线

##  GetFishingLineColor

服务端客户端

###  服务端接口

method in mod.server.component.fishingLineCompServer.FishingLineCompServer

-

描述

获取服务端当前设置的钓鱼线颜色

-

参数

  | 参数名 | 数据类型 | 说明 |
| --- | --- | --- |
   | itemName | str | 鱼竿物品的完整名称，如 "minecraft:fishing_rod" 或自定义鱼竿名称 |

-

返回值

 | 数据类型 | 说明 |
| --- | --- |
  | tuple(float,float,float) | rgb颜色值，每个分量0.0-1.0 |

-

示例

```python
import mod.server.extraServerApi as serverApi
comp = serverApi.GetEngineCompFactory().CreateFishingLine(playerId)
color = comp.GetFishingLineColor("mymod:custom_fishing_rod")
print color

```

###  客户端接口

method in mod.client.component.fishingLineCompClient.FishingLineCompClient

-

描述

获取钓鱼线的颜色

-

参数

  | 参数名 | 数据类型 | 说明 |
| --- | --- | --- |
   | itemName | str | 鱼竿物品的完整名称，如 "minecraft:fishing_rod" 或自定义鱼竿名称 |

-

返回值

 | 数据类型 | 说明 |
| --- | --- |
  | tuple(float,float,float) | rgb颜色值，每个分量0.0-1.0 |

-

示例

```python
import mod.client.extraClientApi as clientApi
comp = clientApi.GetEngineCompFactory().CreateFishingLine(playerId)
color = comp.GetFishingLineColor("mymod:custom_fishing_rod")
print color

```

##  GetFishingLineMax

服务端客户端

###  服务端接口

method in mod.server.component.fishingLineCompServer.FishingLineCompServer

-

描述

获取服务端当前设置的钓鱼线最大长度

-

参数

  | 参数名 | 数据类型 | 说明 |
| --- | --- | --- |
   | itemName | str | 鱼竿物品的完整名称，如 "minecraft:fishing_rod" 或自定义鱼竿名称 |

-

返回值

 | 数据类型 | 说明 |
| --- | --- |
  | float | 最大长度，单位为格 |

-

示例

```python
import mod.server.extraServerApi as serverApi
comp = serverApi.GetEngineCompFactory().CreateFishingLine(playerId)
maxLen = comp.GetFishingLineMax("mymod:custom_fishing_rod")

```

###  客户端接口

method in mod.client.component.fishingLineCompClient.FishingLineCompClient

-

描述

获取钓鱼线的最大长度

-

参数

  | 参数名 | 数据类型 | 说明 |
| --- | --- | --- |
   | itemName | str | 鱼竿物品的完整名称，如 "minecraft:fishing_rod" 或自定义鱼竿名称 |

-

返回值

 | 数据类型 | 说明 |
| --- | --- |
  | float | 最大长度，单位为格 |

-

示例

```python
import mod.client.extraClientApi as clientApi
comp = clientApi.GetEngineCompFactory().CreateFishingLine(playerId)
maxLen = comp.GetFishingLineMax("mymod:custom_fishing_rod")

```

##  SetFishingLineColor

服务端

method in mod.server.component.fishingLineCompServer.FishingLineCompServer

-

描述

设置钓鱼线的颜色，设置后会自动同步到所有客户端。

-

参数

  | 参数名 | 数据类型 | 说明 |
| --- | --- | --- |
   | itemName | str | 鱼竿物品的完整名称，如 "minecraft:fishing_rod" 或自定义鱼竿名称 |
   | color | tuple(float,float,float) | 鱼线颜色rgb，取值[0, 1] |

-

返回值

 | 数据类型 | 说明 |
| --- | --- |
  | bool | 是否设置成功 |

-

示例

```python
import mod.server.extraServerApi as serverApi
comp = serverApi.GetEngineCompFactory().CreateFishingLine(playerId)
comp.SetFishingLineColor("mymod:custom_fishing_rod", (1.0, 0.0, 0.0))

```

##  SetFishingLineMax

服务端

method in mod.server.component.fishingLineCompServer.FishingLineCompServer

-

描述

设置钓鱼线的最大长度（超过此长度会断线），设置后会自动同步到所有客户端。

-

参数

  | 参数名 | 数据类型 | 说明 |
| --- | --- | --- |
   | itemName | str | 鱼竿物品的完整名称，如 "minecraft:fishing_rod" 或自定义鱼竿名称 |
   | maxLength | float | 最大长度，单位为格，钓鱼竿默认值为32.0 |

-

返回值

 | 数据类型 | 说明 |
| --- | --- |
  | bool | 是否设置成功 |

-

示例

```python
import mod.server.extraServerApi as serverApi
comp = serverApi.GetEngineCompFactory().CreateFishingLine(playerId)
comp.SetFishingLineMax("mymod:custom_fishing_rod", 64.0)

```
